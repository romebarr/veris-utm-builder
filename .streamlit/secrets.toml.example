# Copia este archivo a .streamlit/secrets.toml y pon las contrasenas reales.
# secrets.toml esta en .gitignore: nunca se sube al repo.
# En Streamlit Cloud se pega este mismo contenido en Settings -> Secrets.

[passwords]
veris = "cambiar-esta-clave"
agencia = "cambiar-esta-clave"
